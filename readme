设计思路概述：
1，用图来存储所有实体和实体关系
2，定义两个实体互为“同位体”（peer），如果这两个实体可以通过一个实体出发，历经关系名和顺序相同的关系节点序列到达，并且这个序列里不包含other型关系；称这个第三实体为一对同位体的锚实体（anchor）
3，定义一对同位体的同位距离（parDistance)为他们到锚实体需要经过的关系节点数目的最小值，特别地，节点是自己的同位体，和自身的同位距离为0
4，出于工程角度考虑，同位体检索只考虑同位距离不超过3的情况，大于此的不认定为同位体
5，两个实体为“准同位体”（paraPeer），如果这两个实体不互为同位体但是被同样的非other关系连接在同一侧；定义相似度(similarity)为这样的非other关系数目/两者各拥有的关系数目的乘积，非准同位体则是0；定义相似关系对(similarRelationshipPair)为一对准同位体的一对这样的关系；l

如何检索两个实体a、b的可能关系：
定义关系的推测函数f(a,b)为：
0，如果a，b有关系直接相连，直接将其返回，优先返回非other关系；
1，检查与a相连的非other关系彼端有无b的同位体，检查与b相连的非other关系彼端有无a的同位体，如有则取其中同位距离最小的检索结果c，不妨设c是在a相连的关系彼端诸点中检索出来的，则认定a，b的关系应是a，c的关系，返回；
2，检查与a相连的非other关系彼端有无b的准同位体，检查与b相连的非other关系彼端有无a的准同位体，如有则取其中相似度最大的检索结果c，认定a，b的关系是c与其检索源头的关系，返回；
3，认定a，b是other关系，返回；

对推测结果的评价标准，评价函数g(f)：
0，分数越低评价越好
1，若猜得了a，b间直接相关的关系，则返回0分
2，若推测源于其中一节点的关系彼端与另一节点的同位性，则返回同位距离
3，若推测源于其中一节点的关系彼端与另一节点的相似性，则返回相似度的相反数+3
4，若是无法推测导致的给出other结果，返回大数M

如何检索句子中可能存在的关系：
1，分词并在知识库中搜索，剔除知识库中不包含的词
2，对剩余词